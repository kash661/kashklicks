---
import BaseLayout from '../../layouts/BaseLayout.astro';
import Breadcrumbs from '../../components/seo/Breadcrumbs.astro';
import PortfolioCard from '../../components/ui/PortfolioCard.astro';
import Button from '../../components/ui/Button.astro';
import PortfolioFinalCTA from '../../components/global/PortfolioFinalCTA.astro';
import { Image } from 'astro:assets';

import portfolio from '../../data/portfolio.json';
import site from '../../data/site.json';

const portfolioImages = import.meta.glob<{ default: ImageMetadata }>(
  '/src/assets/images/portfolio/*.jpg',
  { eager: true }
);

const ctaImage = (await import('../../assets/images/homepage/civil-service-detail.jpg')).default;

const entries = portfolio.filter((p) => p.category === 'civil-ceremony');

const businessRef = { '@id': `${site.url}/#business` };

const schema = {
  '@context': 'https://schema.org',
  '@type': 'ImageGallery',
  name: 'Toronto Civil Ceremony Photography Gallery, AD Photography',
  description: 'Intimate civil ceremony photography portfolio in Toronto and the GTA by AD Photography.',
  url: new URL('/portfolio/civil-ceremony', Astro.site).href,
  creator: { '@type': 'LocalBusiness', name: 'AD Photography', ...businessRef },
  image: entries.map((entry) => ({
    '@type': 'ImageObject',
    name: `${entry.couple}, Civil Ceremony`,
    description: `Civil ceremony photography of ${entry.couple} at ${entry.location}`,
    contentLocation: { '@type': 'Place', name: entry.location },
  })),
};
---

<BaseLayout
  title="Toronto Civil Ceremony Photography Portfolio"
  description="Browse my Toronto civil ceremony photography portfolio. Intimate courthouse and city hall coverage with the care of a full wedding."
  jsonLd={schema}
>
  <div class="sr-only">
    <Breadcrumbs items={[
      { label: 'Home', href: '/' },
      { label: 'Portfolio', href: '/portfolio' },
      { label: 'Civil Ceremony', href: '/portfolio/civil-ceremony' },
    ]} />
  </div>

  <!-- ═══ GALLERY ═══ -->
  <section class="bg-surface-dim" style="padding: var(--spacing-section) 0;">
    <div class="container-editorial">
      <div class="grid-editorial items-end mb-16">
        <div class="col-span-12 md:col-span-7 reveal">
          <p class="text-label-md text-on-surface-muted mb-4 eyebrow-rule">Civil Ceremony</p>
          <h1 class="text-display-md char-reveal">Toronto Civil Ceremony Stories</h1>
        </div>
        <div class="col-span-12 md:col-span-4 md:col-start-9 mt-6 md:mt-0 reveal stagger-2">
          <p class="text-body-md text-on-surface-variant" style="max-width: 32ch;">
            A small room, a few vows, and a lifetime of feeling. That's what I show up for.
          </p>
        </div>
      </div>

      {entries.length > 0 ? (
        <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-x-10 gap-y-16">
          {entries.map((entry, i) => {
            const imgPath = `/src/assets/images/portfolio/${entry.coverImage}`;
            const img = portfolioImages[imgPath]?.default;
            return img ? (
              <div class:list={['reveal', `stagger-${(i % 5) + 1}`, i % 3 === 1 ? 'lg:mt-24' : '']}>
                <PortfolioCard
                  couple={entry.couple}
                  location={entry.location}
                  category={entry.category}
                  coverImage={img}
                  href={`/portfolio/${entry.slug ?? entry.id}`}
                />
              </div>
            ) : null;
          })}
        </div>
      ) : (
        <div class="text-center py-16">
          <p class="text-body-lg text-on-surface-muted">New work coming soon. Check back or browse my other chapters.</p>
          <div class="mt-8">
            <span class="magnetic inline-block"><Button href="/portfolio" variant="secondary">View All Work</Button></span>
          </div>
        </div>
      )}
    </div>
  </section>

  <!-- ═══ PHILOSOPHY ═══ -->
  <section class="bg-background" style="padding: var(--spacing-section) 0;">
    <div class="container-editorial">
      <div class="max-w-3xl mx-auto text-center reveal">
        <p class="text-quote">
          "The size of the ceremony has nothing to do with the size of the love."
        </p>
      </div>
    </div>
  </section>

  <PortfolioFinalCTA
    title="Book coverage."
    body="Planning a civil ceremony? Let's make sure every moment, no matter how small, is kept."
    ctaLabel="Begin The Conversation"
    ctaHref="/contact?service=civil-ceremony"
    image={ctaImage}
    imageAlt="Toronto civil ceremony couple portrait by AD Photography"
    imageCaption="A Quiet Ceremony"
  />
</BaseLayout>
