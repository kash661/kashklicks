---
interface Props {
  items: { question: string; answer: string }[];
}

const { items } = Astro.props;
---

<div class="space-y-0" style="gap: 0;">
  {items.map((item, i) => (
    <details class="group" style={`padding: var(--spacing-element) 0;${i === 0 ? 'padding-top: 0;' : ''}`}>
      <summary class="flex items-center justify-between cursor-pointer list-none">
        <h3 class="text-body-lg font-medium pr-8">{item.question}</h3>
        <span class="text-on-surface-variant text-2xl cubic-ease group-open:rotate-45" style="transition-duration: var(--duration-slow);" aria-hidden="true">+</span>
      </summary>
      <div class="text-body-md text-on-surface-variant" style="padding-top: 8px; max-width: 65ch;">
        <p>{item.answer}</p>
      </div>
    </details>
  ))}
</div>
